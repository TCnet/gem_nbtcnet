require "spec_helper"

RSpec.describe GemNbtcnet do
  it "has a version number" do
    expect(GemNbtcnet::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
